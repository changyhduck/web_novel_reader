# 小說 Markdown 整理規範

本文件供作者與 AI 整理小說章節使用。目標是將既有小說轉成可供本專案閱讀器載入的 Markdown 檔案，保留原文內容與順序。

## 1. 整理原則

- 除非使用者另外要求改寫或校對，僅調整排版，不增刪劇情、修改措辭、變更人名、翻譯或轉換繁簡體。
- 保留原文對話、標點、敘事視角及段落順序。不自行補寫章名、幕名、作者或圖片內容。
- 對缺字、疑似錯字、無法判斷的段落或圖片位置，在章節檔案外列出待確認事項，不把猜測寫進正文。
- 每章一個 `.md` 檔案，使用 UTF-8 編碼。不得將多章合併為一檔，或把一章任意拆成多章。
- 檔案只包含小說內容，不加入 AI 的說明、整理報告、檔名標籤或整份文件的程式碼圍欄。

## 2. 目錄與檔名

```text
web/novel/小說名稱/
├── meta.json
└── 001_第一幕_幕標題/
    ├── 001_第1章_章標題.md
    ├── 002_第2章_章標題.md
    └── images/
        ├── 001_插圖01.jpg
        └── 002_插圖01.jpg
```

- 幕目錄格式：`排序序號_幕標示_幕標題`。
- 章節檔名格式：`排序序號_章標示_章標題.md`。
- 排序序號至少三位數，例如 `001`、`002`、`010`；同一層不可重複，以數值大小決定閱讀順序。
- 章節排序序號表示幕內順序；顯示章號可跨幕接續，例如第二幕第一個檔案可為 `001_第8章_重逢.md`。
- Windows 檔名不得包含 `\ / : * ? " < > |`，也不可使用保留名稱或以空格、句點結尾。原標題含半形冒號時可在檔名改用全形冒號 `：`；其他不合法字元的替換須在整理報告中說明。
- 未提供幕、章名稱或排序時，先列出缺少的資料，不憑空命名。既有檔案整理時，不自行重新編號或改名，以免原閱讀紀錄失效。

閱讀器會將 `001_第1章_夜裡的訪客.md` 顯示為「第1章：夜裡的訪客」。檔名前的序號及副檔名不顯示；章標示後的第一個底線變成全形冒號，標題內其餘底線保留。

## 3. 章名與小節標題

章名由檔名提供，正文不要重複放置章名，也不要加入第一級標題 `#`。

若來源開頭有與該檔案對應的獨立章名行，將其資訊用於檔名並從正文移除；無法確定是否為章名時列為待確認，不任意刪除。故事中引用的章名不屬於這項移除範圍。

原文確有小節標題時，可使用二級及三級標題，標題前後各留一個空白行：

```markdown
## 夜半

這裡是小節內文。

### 留在桌上的信

這裡是下一層小節的內文。
```

不要為了整理而創造新的小節標題。

## 4. 段落與換行

- 不同段落之間使用一個空白行。
- 一般正文每段盡量寫在同一行，不按畫面寬度手動折行。
- 同一段內的單次換行會呈現為空格，不保證顯示斷行。來源若因 PDF 或文字編輯器而折行，只有在確定屬於同一段時才接回，並保留原本必要的字間空格。
- 同一段內必須斷行時，使用行尾反斜線 `\`，例如詩句、信件或地址；不使用難以辨識的行尾雙空格。
- 不以全形空格、Tab 或連續空格製作首行縮排、置中或對齊；排版由閱讀器處理。正文四個前導空格可能被當作程式碼區塊，應避免。
- 多個空白行不代表更多留白。若原稿藉大量空白表達場景切換但意圖不明，列為待確認。

範例：

```markdown
雨停了，他仍站在門前。

桌上的紙條只寫著兩行字：\
明天，老地方見。
```

## 5. 對話、強調與場景分隔

- 原稿每位說話者已有獨立段落時，保留其段落。
- 一般對話直接使用原文引號，例如 `「……」`，不要自動改成清單、引用區塊或「角色名：」形式。
- 保留原文的引號風格；沒有校對授權時，不一律改換標點。
- 粗體 `**文字**`、斜體 `*文字*` 僅用於原稿已有的強調或使用者指定的位置。
- 明確的場景分隔可使用獨立一行的 `---`，前後各留一個空白行。若原文分隔符本身有敘事意義，保留原符號，不任意替換。
- 故事中的書信或引文，只有原稿明確採引用排版時才使用 `>`。引用內的段落仍需保留。

```markdown
「你回來了。」她說。

「嗯。」他放下行李，「路上耽擱了一會兒。」

---

天亮時，街道已經恢復平靜。
```

## 6. 插圖

使用標準 Markdown 圖片語法，圖片獨立成段，前後各留一個空白行：

```markdown
他推開窗，看見遠方的燈火。

![窗外的夜景](images/001_插圖01.jpg)

風從窗邊吹進房間。
```

- 路徑以目前章節檔案所在的幕目錄為基準，通常使用 `images/檔名`。
- 使用 `/` 分隔路徑，不使用 Windows 反斜線、磁碟機路徑或網站根目錄絕對路徑。
- 第一版使用本地 JPEG、PNG、WebP 或 GIF 檔案，不引用外部圖片網址，不嵌入 Base64 圖片。
- 圖片檔名以所屬章節排序序號開頭，例如 `001_插圖01.jpg`、`001_插圖02.jpg`；檔名大小寫與實際檔案完全一致。
- 若既有圖片檔名含空格，使用角括號包住路徑，例如 `![插圖](<images/001_插圖 01.jpg>)`；不要自行更動檔名後留下失效連結。
- 替代文字使用已知的簡短描述；沒有圖片描述時可用「本章插圖」，不要猜測畫面。替代文字不是正文中的可見圖說；原稿有圖說時保留為圖片下方獨立段落。
- 不自行插入、刪除或移動插圖。缺少圖片或無法確定插入位置時，在整理報告中列出，不在正文填入假圖片路徑。
- 圖片尺寸與置中由閱讀器處理，不使用 HTML 或 CSS 控制圖片。

## 7. 不放入章節的內容

- 不加入 YAML front matter（檔案開頭由 `---` 包住的作者、章名等資料）。小說作者、封面、簡介與別名放在小說根目錄的 `meta.json`。
- 不手工加入整本小說目錄、Home、上一章／下一章連結或閱讀設定。這些由閱讀器產生。
- 不加入 `<script>`、`<style>`、`<iframe>`、其他原始 HTML 或內嵌事件。需要文字效果時使用規範中的 Markdown 語法。
- 不用程式碼區塊包住一般小說段落，也不加入為排版而設的表格。
- 不增加原文沒有的摘要、心得、翻譯、註解或 AI 生成聲明。

若原文含 `*`、`_`、`#` 等符號而會被誤解為 Markdown 格式，使用反斜線跳脫，確保讀者看到原本文字。原稿確實含有特殊版面、表格或程式碼時，不擅自丟棄，應另外提出待確認事項。

## 8. 完整章節範例

假設檔名為 `001_第1章_夜裡的訪客.md`，檔案內容如下。這是示範文字，不應補入使用者的小說：

```markdown
雨停了，屋簷上的水仍一滴滴落下。

「有人在嗎？」門外傳來聲音。

他合上書，走向玄關。

![本章插圖](images/001_插圖01.jpg)

門縫下壓著一張紙條，上面寫著：\
請在天亮以前離開。

---

翌日清晨，街上沒有任何腳印。
```

## 9. 交付前檢查

- [ ] 每章獨立成檔，幕與章排序、顯示名稱符合使用者提供的資料。
- [ ] 檔案使用 UTF-8，正文沒有重複章名或第一級標題。
- [ ] 除明確獨立章名外，原文內容、順序與標點均保留，沒有擅自改寫。
- [ ] 段落以空白行分隔；明確斷行使用反斜線，沒有手工縮排或意外程式碼區塊。
- [ ] 圖片位置、檔名、相對路徑、大小寫與原稿及實際檔案一致。
- [ ] 沒有 front matter、原始 HTML、導航連結或 AI 說明混入正文。
- [ ] 所有不確定事項另外列出，未用猜測填補。
- [ ] 若可存取專案工具，執行索引驗證並記錄結果；不可存取檔案或工具時，明確標示哪些檢查尚未完成，不宣稱已驗證。

## 10. 可直接提供給 AI 的指令

> 請依照隨附的《小說 Markdown 整理規範》整理我提供的小說。這是格式整理工作，不是改寫；請保留原文內容、順序與標點，每章輸出一個 UTF-8 Markdown 檔案。章名取自指定檔名，不在正文重複。遵守段落、對話、換行與圖片相對路徑規則，不加入 front matter 或 HTML。請先根據我提供的幕名、章名、排序及圖片清單整理；缺少必要資訊或遇到不確定內容時，另外列為待確認，不自行創作。若能建立檔案，請直接輸出章節檔與獨立的整理報告；若只能在對話中交付，請在各章程式碼區塊外標明檔名，區塊內只放章節內容，儲存時不要包含外層圍欄。報告請列出實際調整、待確認事項與未能執行的檢查。

使用時一併提供原文、目標幕目錄、各章檔名與排序，以及圖片檔名和插入位置。只有本規範而沒有小說原文時，AI 不應自行生成正文。
